
    <!--============================= HEADER =============================-->
    <div class="nav-menu">
        <div class="bg transition">
            <div class="container-fluid fixed">
                <div class="row">
                    <div class="col-md-12">
                        <nav class="navbar navbar-expand-lg navbar-light">
                            <span class="navbar-brand" href="index.html">Wanderlust</span>
                            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                                aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                                <span class="icon-menu"></span>
                            </button>
                            <div class="collapse navbar-collapse justify-content-end" id="navbarNavDropdown">
                                <ul class="navbar-nav">
                                    <li class="nav-item dropdown">
                                        <!-- <a class="nav-link" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown"
                                            aria-haspopup="true" aria-expanded="false">
                                            Explore
                                            <span class="icon-arrow-down"></span>
                                        </a> -->
                                        <!-- <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                                            <a class="dropdown-item" href="#">Action</a>
                                            <a class="dropdown-item" href="#">Another action</a>
                                            <a class="dropdown-item" href="#">Something else here</a>
                                        </div> -->
                                    </li>
                                   
                                    <li class="nav-item active">
                                        <a class="btn btn-outline-light top-btn" href="/logout">Logout</a>
                                    </li>
                                    <li class="nav-item active">
                                        <div id="navcreate"></div>
                                        
                                    </li>
                                    <li>
                                        <a href="/members" class="btn btn-outline-light top-btn"><span class="ti-plus"></span>
                                            Add Listing</a></li>

                                </ul>
                            </div>
                        </nav>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- SLIDER -->
    <section class="slider d-flex align-items-center">
        <!-- <img src="images/slider.jpg" class="img-fluid" alt="#"> -->
        <div class="container">
            <div class="row d-flex justify-content-center">
                <div class="col-md-12">
                    <div class="slider-title_box">
                        <div class="row">
                            <div class="col-md-12">
                                <div class="slider-content_wrap">
                                    <h1>Wanderlust</h1>
                                    <h5>Finding your home away from home. Around the World.</h5>
                                </div>
                            </div>
                        </div>
                        <div class="row d-flex justify-content-center">
                            <div class="col-md-10">
                                <form class="form-wrap mt-4">
                                    <div class="btn-group" role="group" aria-label="Basic example">
                                        <input type="text" id="citySearch" placeholder="City" class="btn-group1">
                                        <input type="text" id="stateSearch" placeholder="State" class="btn-group2">
                                        <input type="text" id="startDateSearch" placeholder="Start Date" class="btn-group2">
                                        <input type="text" id="endDateSearch" placeholder="End Date" class="btn-group2">
                                        <button type="submit" id="searchButton" class="btn-form"><span class="icon-magnifier search-icon"></span>SEARCH<i
                                                class="pe-7s-angle-right"></i></button>
                                    </div>
                                </form>
                                
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!--// SLIDER -->
    <!--//END HEADER -->


    <!--============================= FEATURED PLACES =============================-->
    <section class="main-block light-bg showw" style="display:none">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-md-5">
                    <div class="styled-heading">
                        <h3>Search Results</h3>
                    </div>
                </div>
            </div>
            <div class="row" id="created">
                 {{!-- <div id="created">


                </div> --}}
            
            </div>
        </div>                       
    </section>
    <!--//END FEATURED PLACES -->


    <!--============================= YELP API =============================-->


    <section class="main-block light-bg showw" style="display:none">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-md-5">
                    <div class="styled-heading">
                        <h3>Check out these cool places</h3>
                    </div>
                </div>
            </div>
            <div class="row" id="yelp"> </div>
             <div class="row" id="yelp1"></div>
              <div class="row" id="yelp2"></div>
        </div>
    </section>

    <!--============================= END OF YELP =============================-->




    <!--============================= FOOTER =============================-->
    <footer class="main-block dark-bg">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="copyright">
                        <!-- Link back to Colorlib can't be removed. Template is licensed under CC BY 3.0. -->
                        <p>Copyright &copy; 2018 Wanderlust. All rights reserved</p>
                        <!-- Link back to Colorlib can't be removed. Template is licensed under CC BY 3.0. -->
                        <ul>
                            <li><a href="#"><span class="ti-facebook"></span></a></li>
                            <li><a href="#"><span class="ti-twitter-alt"></span></a></li>
                            <li><a href="#"><span class="ti-instagram"></span></a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </footer>
    <!--//END FOOTER -->





    <!-- jQuery, Bootstrap JS. -->
    <!-- jQuery first, then Popper.js, then Bootstrap JS -->
    <!-- <script src="js/jquery-3.2.1.min.js"></script> -->

    <!-- <script src="js/popper.min.js"></script>
    <script src="js/bootstrap.min.js"></script> -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.22.2/moment.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <!-- <script src="js/index.js"></script> -->

    <script type="text/javascript">


         $( document ).ready(function() {

             $.get("/api/user_data").then(function (data){
                 console.log(data)

           

                
             

                var navLink = $("<a>")
                navLink.addClass("btn btn-outline-light top-btn")
                navLink.attr("href", "/userlistings/" + data.id)
                var navSpan = $("<span>")
                navSpan.addClass("ti-eye")
                navLink.append(navSpan)
                navLink.text("My Listings")
                

                $("#navcreate").append(navLink)
        





             })


         })



























        $("#searchButton").on("click", function (event) {

            event.preventDefault();


            $(".showw").css("display", "block")

            var citySearch = $("#citySearch").val()
            var stateSearch = $("#stateSearch").val()
            var startSearch = $("#startDateSearch").val()

            var endSearch = $("#endDateSearch").val()


            $.get("/search/" + citySearch + "/" + stateSearch + " /" + startSearch + "/" + endSearch).then(function (data) {
                console.log(data)

                for (var x = 0; x < data.length; x++) {

                    var $newDiv = $("<div class='col-md-4 featured-responsive'>");
                    var featuredWrap = $("<div class='featured-place-wrap'>");
                    var newImage = $("<img>")
                    newImage.attr("src", data[x].Img1);
                    newImage.attr("class", "img-fluid");
                    // var link1 = $("<a>")
                    // link1.attr("href", "#");

                    featuredWrap.append(newImage);
                    $newDiv.append(featuredWrap);

                    var cardBox = $("<div class='featured-title-box'>");
                    var cardHeader = $("<h6>")
                    cardHeader.text(data[x].listingName);

                    cardBox.append(cardHeader);


                    var bedroomLine = $("<p>")
                    bedroomLine.text(data[x].BedroomNumber + " Bedroom" + " |");

                    var bathroomLine = $("<p>")
                    bathroomLine.html(`&nbsp; ${data[x].BathroomNumber} Bathroom`);

                    cardBox.append(bedroomLine)
                    cardBox.append(bathroomLine)


                    var lists = $("<ul>")

                    var item1 = $("<li>")
                    var span1 = $("<span>")
                    var info = $("<p>")
                    span1.addClass("icon-location-pin")
                    item1.append(span1)
                    info.text(data[x].City + ", " + data[x].State)
                    item1.append(info)


                    lists.append(item1);

                    var item2 = $("<li>")
                    var span2 = $("<span>")
                    var info2 = $("<p>")
                    span2.addClass("icon-wallet")
                    item2.append(span2)
                    info2.text("$" + data[x].PricePerNight + "/night")
                    item2.append(info2)

                    lists.append(item2);



                    cardBox.append(lists);



                    var moreInfoButton = $("<div class='bottom-icons'>")
                    var buttonLink = $("<a>")
                    buttonLink.attr("href", "/listing/" + data[x].id)
                    var moreInfoButtonOther = $("<button>")
                    moreInfoButtonOther.attr("value", data[x].id)
                    moreInfoButton.addClass("infoBtn")
                    
                   buttonLink.append(moreInfoButtonOther);
                    moreInfoButton.append(buttonLink)
                    moreInfoButtonOther.text("SEE MORE INFO");

                    cardBox.append(moreInfoButton);



                    featuredWrap.append(cardBox);
                    // featuredWrap.append(link1);
                    $newDiv.append(featuredWrap);


                    $("#created").append($newDiv)




                }



            });

            $.get("/api/yelp1/" + citySearch + "," + stateSearch).then(function (data) {

                console.log(data.jsonBody)

                for (var x = 0; x < data.jsonBody.businesses.length; x++) {

                    var $newDiv = $("<div class='col-xl-4 featured-responsive'>");
                    var featuredWrap = $("<div class='featured-place-wrap'>");
                    var newImage = $("<img>")
                    newImage.attr("src", data.jsonBody.businesses[x].image_url);
                    newImage.attr("class", "img-fluid");
                    var link1 = $("<a>")
                    link1.attr("href", data.jsonBody.businesses[x].url);

                    link1.append(newImage);
                    $newDiv.append(featuredWrap);

                    var cardBox = $("<div class='featured-title-box'>");
                    var cardHeader = $("<h6>")
                    cardHeader.text(data.jsonBody.businesses[x].name);

                    cardBox.append(cardHeader);


                    var bedroomLine = $("<p>")
                    bedroomLine.text(data.jsonBody.businesses[x].price);

                    var bathroomLine = $("<p>")
                    bathroomLine.html(`&nbsp; ${data.jsonBody.businesses[x].rating}`);

                    cardBox.append(bedroomLine)
                    cardBox.append(bathroomLine)


                    var lists = $("<ul>")

                    var item1 = $("<li>")
                    var span1 = $("<span>")
                    var info = $("<p>")
                    span1.addClass("icon-location-pin")
                    item1.append(span1)
                    info.text(data.jsonBody.businesses[x].display_phone)
                    item1.append(info)


                    lists.append(item1);

                    var item2 = $("<li>")
                    var span2 = $("<span>")
                    var info2 = $("<p>")
                    span2.addClass("icon-wallet")
                    item2.append(span2)
                    info2.text(data.jsonBody.businesses[x].categories[0].alias)
                    item2.append(info2)

                    lists.append(item2);



                    cardBox.append(lists);



                    var moreInfoButton = $("<div class='bottom-icons'>")
                    var moreInfoButtonOther = $("<div class='closed-now'>")
                    moreInfoButton.append(moreInfoButtonOther);
                    moreInfoButtonOther.text("SEE MORE INFO");

                    cardBox.append(moreInfoButton);



                    link1.append(cardBox);
                    featuredWrap.append(link1);
                    $newDiv.append(featuredWrap);


                    $("#yelp").append($newDiv)




                }

            })

            $.get("/api/yelp2/" + citySearch + "," + stateSearch).then(function (data) {

                console.log(data.jsonBody)

                for (var x = 0; x < data.jsonBody.businesses.length; x++) {

                    var $newDiv = $("<div class='col-md-4 featured-responsive'>");
                    var featuredWrap = $("<div class='featured-place-wrap'>");
                    var newImage = $("<img>")
                    newImage.attr("src", data.jsonBody.businesses[x].image_url);
                    newImage.attr("class", "img-fluid");
                    var link1 = $("<a>")
                    link1.attr("href", data.jsonBody.businesses[x].url);

                    link1.append(newImage);
                    $newDiv.append(featuredWrap);

                    var cardBox = $("<div class='featured-title-box'>");
                    var cardHeader = $("<h6>")
                    cardHeader.text(data.jsonBody.businesses[x].name);

                    cardBox.append(cardHeader);


                    var bedroomLine = $("<p>")
                    bedroomLine.text(data.jsonBody.businesses[x].price);

                    var bathroomLine = $("<p>")
                    bathroomLine.html(`&nbsp; ${data.jsonBody.businesses[x].rating}`);

                    cardBox.append(bedroomLine)
                    cardBox.append(bathroomLine)


                    var lists = $("<ul>")

                    var item1 = $("<li>")
                    var span1 = $("<span>")
                    var info = $("<p>")
                    span1.addClass("icon-location-pin")
                    item1.append(span1)
                    info.text(data.jsonBody.businesses[x].display_phone)
                    item1.append(info)


                    lists.append(item1);

                    var item2 = $("<li>")
                    var span2 = $("<span>")
                    var info2 = $("<p>")
                    span2.addClass("icon-wallet")
                    item2.append(span2)
                    info2.text(data.jsonBody.businesses[x].categories[0].alias)
                    item2.append(info2)

                    lists.append(item2);



                    cardBox.append(lists);



                    var moreInfoButton = $("<div class='bottom-icons'>")
                    var moreInfoButtonOther = $("<div class='closed-now'>")
                    moreInfoButton.append(moreInfoButtonOther);
                    moreInfoButtonOther.text("SEE MORE INFO");

                    cardBox.append(moreInfoButton);



                    link1.append(cardBox);
                    featuredWrap.append(link1);
                    $newDiv.append(featuredWrap);


                    $("#yelp1").append($newDiv)




                }

            })

            $.get("/api/yelp3/" + citySearch + "," + stateSearch).then(function (data) {

                console.log(data.jsonBody)

                for (var x = 0; x < data.jsonBody.businesses.length; x++) {

                    var $newDiv = $("<div class='col-md-4 featured-responsive'>");
                    var featuredWrap = $("<div class='featured-place-wrap'>");
                    var newImage = $("<img>")
                    newImage.attr("src", data.jsonBody.businesses[x].image_url);
                    newImage.attr("class", "img-fluid");
                    var link1 = $("<a>")
                    link1.attr("href", data.jsonBody.businesses[x].url);

                    link1.append(newImage);
                    $newDiv.append(featuredWrap);

                    var cardBox = $("<div class='featured-title-box'>");
                    var cardHeader = $("<h6>")
                    cardHeader.text(data.jsonBody.businesses[x].name);

                    cardBox.append(cardHeader);


                    var bedroomLine = $("<p>")
                    bedroomLine.text(data.jsonBody.businesses[x].price);

                    var bathroomLine = $("<p>")
                    bathroomLine.html(`&nbsp; ${data.jsonBody.businesses[x].rating}`);

                    cardBox.append(bedroomLine)
                    cardBox.append(bathroomLine)


                    var lists = $("<ul>")

                    var item1 = $("<li>")
                    var span1 = $("<span>")
                    var info = $("<p>")
                    span1.addClass("icon-location-pin")
                    item1.append(span1)
                    info.text(data.jsonBody.businesses[x].display_phone)
                    item1.append(info)


                    lists.append(item1);

                    var item2 = $("<li>")
                    var span2 = $("<span>")
                    var info2 = $("<p>")
                    span2.addClass("icon-wallet")
                    item2.append(span2)
                    info2.text(data.jsonBody.businesses[x].categories[0].alias)
                    item2.append(info2)

                    lists.append(item2);



                    cardBox.append(lists);



                    var moreInfoButton = $("<div class='bottom-icons'>")
                    var moreInfoButtonOther = $("<div class='closed-now'>")
                    moreInfoButton.append(moreInfoButtonOther);
                    moreInfoButtonOther.text("SEE MORE INFO");

                    cardBox.append(moreInfoButton);



                    link1.append(cardBox);
                    featuredWrap.append(link1);
                    $newDiv.append(featuredWrap);


                    $("#yelp2").append($newDiv)




                }

            })

           

        })













    </script>

